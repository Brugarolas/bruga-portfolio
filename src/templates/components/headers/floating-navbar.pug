mixin floating-navbar(selected = 1)
  nav.floating-navbar
    div.floating-navbar__button
      i.floating-navbar__button__icon
    div.floating-navbar__background
      +link-local('Home', 1, selected)
      +link-local('About me', 2, selected)
